[project]
name = "fhds"
version = "1.4.1"
description = "Forza Horizon DualSense adaptive triggers (Steam keeps rumble)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hidapi>=0.15.0",
    "psutil>=7.2.2",
    "textual>=8.2.5",
]

[tool.zuv]
entry = "main.py"
volume = "data"

[tool.ruff]
line-length = 120

[tool.uv.sources]
