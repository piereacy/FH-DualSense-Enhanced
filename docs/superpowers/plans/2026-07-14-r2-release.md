# FH-DualSense-Enhanced R2 发布实施计划

**Goal:** 将已经实现并完成实机验证的 Enhanced R2 按独立 `R` 版本体系构建、验证并发布为 GitHub Release。

**Architecture:** 保持触觉算法、HID、遥测和默认参数不变。`src/pyproject.toml` 使用 PEP 440 内部版本 `2`，显示层、构建脚本和 Release workflow 将其映射为公开版本 `R2`。保留固定名称的 ZUV 资产和 R1 启动器兼容链路。

**Reference:** `docs/superpowers/specs/2026-07-14-r2-release-design.md`

## Task 1：锁定发布契约

- [x] 先修改 `tests/test_enhanced_distribution.py`，锁定内部版本 `2`、公开版本 `R2`、GUI/TUI 显示、构建产物名称和来源声明。
- [x] 锁定 workflow 对 `R2`、`release R2`、`R2-preview` 和历史 `v...` 触发方式的兼容。
- [x] 锁定固定 ZUV 资产名 `FH-DualSense-Enhanced.zuv.py`。
- [x] 运行定向测试并确认其先因旧发布身份失败。

## Task 2：实现版本身份与构建命名

- [x] 将 `src/pyproject.toml` 的内部版本改为 `2`，同步锁文件。
- [x] 在配置/版本读取层提供从内部版本到公开 `R2` 的单一映射。
- [x] 将 GUI 和 TUI 的版本文本改为公开 `R2`。
- [x] 将 Windows EXE、Windows VERSIONINFO 和 Linux ELF 的公开名称改为 `R2`。

## Task 3：更新 Release workflow

- [x] 支持稳定 tag `R2` 和提交标题 `release R2`。
- [x] 保留历史 `vX.Y.Z` 与 `vX.Y.Z.postN` 兼容。
- [x] 让手动触发生成可移动的 `R2-preview` prerelease。
- [x] 为仓库未交付 push 事件的情况提供显式手动 `stable` 恢复入口。
- [x] 确保 Windows/Linux 产物、标题和 Release body 均使用公开版本名。
- [x] 保留 bundle、固定 ZUV 资产、standalone、Data Out、防火墙和 Linux udev 说明。

## Task 4：同步三语文档

- [x] 更新根 `README.md` 同页中文、English、日本語内容。
- [x] 更新 `docs/ReadmeEN.md`、`docs/ReadmeJA.md` 和 `packaging/release/README.txt`。
- [x] 说明 R2 版本体系、上游 `1.6.2`、HorizonHaptics `1.3.0` 和历史 R1 `1.6.2.post1`。
- [x] 记录 R2 动态 wheelspin、四种材质频带、GT7 风格 ABS wall、实验性设置与已完成验证边界。

## Task 5：本地质量门禁

- [x] 运行发布契约定向测试。
- [x] 运行 `uv run --project src pytest -q`。
- [x] 运行 `uv run --project src python -m compileall -q src/modules src/lang`。
- [x] 运行 `git diff --check` 并检查完整 diff。
- [x] 更新 `docs/PROJECT_STATE.md`，只记录实际完成的实现与验证。

## Task 6：构建并验证本地产物

- [x] 使用 `UPDATE_REPO=piereacy/FH-DualSense-Enhanced` 构建 update-enabled ZUV。
- [x] 使用 `uvx zuv inspect` 检查内部版本、入口、volume 和 update repo。
- [x] 构建 `FH-DualSense-Enhanced-R2.exe`。
- [x] 检查 EXE 的 VERSIONINFO、应用图标和 `--help` 启动冒烟。
- [x] 确认构建产物未被误提交。

## Task 7：推送并验证预发布

- [x] 提交发布准备改动。
- [x] 将功能分支快进合并到 `main` 并推送 `origin/main`。
- [x] 手动触发 Release workflow，生成 `R2-preview`。
- [x] 等待 Windows、Linux 和 Release jobs 全部成功，核对资产名称和 prerelease 元数据。
- [x] 确认 preview CI 没有失败项，无需进入修复重跑分支。

## Task 8：发布稳定 R2

- [x] 在已经通过预发布 CI 的代码上创建并推送 tag `R2`；因 push event 未交付，tag 最终更新到带手动 stable 恢复入口的 `1cc4520`。
- [x] 等待稳定 Release workflow 完成。
- [x] 核验标题 `FH-DualSense-Enhanced R2`、tag、正文、Windows/Linux/ZUV/bundle 资产和源码归档。
- [x] 下载关键资产做最终可访问性检查。
- [x] 确认 Git 工作区干净，并报告最终提交、tag、Release 链接和验证结果。
